# Task 6 report — selected package Astro layouts and blocks

## Scope

Implemented Task 6 only. The website now validates the immutable publication snapshot against the build-selected Site Package, resolves package renderer overrides before the core fallback, renders public CMS pages through the selected package layout, and derives metadata and media loading behavior from the snapshot. There is no runtime CMS API fetch, browser package selection, admin change, destination change, or Task 7 reference package implementation.

`website/src/layouts/BaseLayout.astro` is the one incidental directly coupled file omitted from the plan's file list. Task 6 explicitly requires snapshot social-image metadata, and BaseLayout owns the document `<head>`; its change is limited to optional Open Graph image fields and Twitter card selection.

The unrelated untracked `.playwright-cli/`, `.ux-audit/`, `output/`, and `webapp/.21st/` directories were preserved and are not staged.

## Plan inconsistency ruling

Task 6 Step 1 mentioned an `estimateCalculator` renderer and package header/footer even though the approved sequential plan creates those concrete artifacts in Task 7. The staged `vibe-core` package intentionally has an identity layout and no bespoke renderer. Per the parent-agent ruling, Task 6 implements and tests the generic layout/renderer dispatch with an injected registry fixture, while the real calculator and bespoke header/footer remain Task 7. The HTML build fixture proves that a CMS `/` page uses the selected layout boundary and replaces the hard-coded template homepage and metadata.

## RED

Tests were written before production changes, then run with:

```text
bun run test:website
10 pass, 2 fail, 2 module errors
Export named 'resolvePageMetadata' not found in website/src/cms/snapshot.ts
Export named 'createBlockRendererResolver' not found in website/src/cms/block-registry.ts
```

The failures were caused by the missing Task 6 boundaries, not fixture or syntax errors.

## GREEN

- `selectedWebsiteSnapshotSchema` composes `selectedPublicationSnapshotSchema` with exact literals for the staged descriptor. Snapshot package ID, version, or schema-version mismatch fails before rendering.
- The renderer registry is generated from `selectedBlockDefinitions`; package renderers can override a registered core semantic type, registered bespoke types require a package renderer, unknown renderer registrations and unknown requested types fail closed.
- The former `Block.astro` renderer is now `CoreBlock.astro`; the new `Block.astro` performs only package/core dispatch.
- `SitePage.astro` receives the complete snapshot and page, passes settings, logo, menus, collections, media, page, snapshot, and the validated block renderer component to the selected package layout, and uses the package layout around core-controlled validated content.
- Home and dynamic public pages use page/default SEO title, description, canonical mode/URL, social image, and `noIndex`. The hard-coded Vibe metadata remains only as the no-snapshot template fallback.
- Open Graph social-image metadata includes URL, alt text, and known dimensions. CMS images reserve known width/height, use responsive `sizes`, make only the first hero image eager/high priority, and lazy-load later images.
- Website declares direct workspace/runtime dependencies on the selected package and Zod; `bun.lock` contains only those website dependency edges.

Focused GREEN evidence:

```text
bun test website/tests/cms-rendering.test.ts website/tests/cms-site-package.test.ts
7 pass, 0 fail
```

The build fixture runs a real Astro build from a temporary immutable snapshot and checks selected-package identity, CMS title/description/noIndex/canonical/social-image output, server-rendered CMS copy, template removal, reserved image dimensions, responsive sizes, and eager/lazy behavior.

## Full verification

Fresh verification on the final implementation tree:

```text
bun run test:website
17 pass, 0 fail

bun run typecheck:website
0 errors, 0 warnings, 1 existing hint

bun run build:website
production build completed, exit 0

bun run architecture:check
Architecture check passed (452 source files)

bun install --lockfile-only --offline --frozen-lockfile
Saved bun.lock (1111 packages), exit 0

git diff --check
exit 0
```

Astro type checking continues to report the pre-existing deprecated `verticalAlign` hint in `website/src/components/ui/chart.tsx`. The production build continues to print the existing large-chunk advisory. Task 6 does not touch either source.

## Commit

Required commit message: `feat(cms): render selected site package in Astro`
