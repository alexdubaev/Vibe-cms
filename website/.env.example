# Private backend origin used only by the Node preview runtime. Never expose as PUBLIC_*.
CMS_BACKEND_ORIGIN=https://api.example.test

# Build-time public metadata for static pages.
PUBLIC_WEBSITE_URL=https://www.example.test
PUBLIC_WEBAPP_URL=https://app.example.test
